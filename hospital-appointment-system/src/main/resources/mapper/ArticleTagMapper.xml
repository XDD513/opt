<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.ArticleTagMapper">

    <!-- ResultMap 定义 -->
    <resultMap id="ArticleTagResultMap" type="com.hospital.entity.ArticleTag">
        <id property="id" column="id"/>
        <result property="articleId" column="article_id"/>
        <result property="tagName" column="tag_name"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 根据文章ID查询所有标签名称（只查询article_id不为NULL的记录） -->
    <select id="selectTagNamesByArticleId" resultType="java.lang.String">
        SELECT tag_name
        FROM content_article_tag
        WHERE article_id = #{articleId} AND article_id IS NOT NULL
        ORDER BY created_at ASC
    </select>

    <!-- 根据标签名称查询所有文章ID（只查询article_id不为NULL的记录） -->
    <select id="selectArticleIdsByTagName" resultType="java.lang.Long">
        SELECT article_id
        FROM content_article_tag
        WHERE tag_name = #{tagName} AND article_id IS NOT NULL
        ORDER BY created_at DESC
    </select>

    <!-- 批量插入文章标签关联 -->
    <insert id="insertBatch">
        INSERT INTO content_article_tag (id, article_id, tag_name, created_at, updated_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.articleId}, #{item.tagName}, #{item.createdAt}, #{item.updatedAt})
        </foreach>
    </insert>

    <!-- 根据文章ID将article_id设置为NULL（软删除，保留标签记录） -->
    <update id="setArticleIdToNull">
        UPDATE content_article_tag
        SET article_id = NULL, updated_at = NOW()
        WHERE article_id = #{articleId}
    </update>

    <!-- 根据文章ID和标签名称删除关联 -->
    <delete id="deleteByArticleIdAndTagName">
        DELETE FROM content_article_tag
        WHERE article_id = #{articleId} AND tag_name = #{tagName}
    </delete>

    <!-- 查询所有不重复的标签名称（包括article_id为NULL的记录） -->
    <select id="selectAllDistinctTagNames" resultType="java.lang.String">
        SELECT DISTINCT tag_name
        FROM content_article_tag
        ORDER BY tag_name ASC
    </select>

</mapper>

